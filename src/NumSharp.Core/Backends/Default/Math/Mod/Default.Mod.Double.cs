//Generated by Regex Templating Engine at 11/08/2019 19:26:50 UTC
//template source: C:\Users\Eli-PC\Desktop\SciSharp\NumSharp\src\NumSharp.Core\Backends\Default\Math\Templates\Default.Op.General.template.cs

using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Diagnostics;
using System.Diagnostics.CodeAnalysis;
using System.Linq;
using System.Text;
using System.Numerics;
using System.Runtime.CompilerServices;
using System.Threading.Tasks;
using NumSharp.Backends.Unmanaged;
using NumSharp.Utilities;
using NumSharp.Utilities.Maths;

namespace NumSharp.Backends
{
    //v2
    public partial class DefaultEngine
    {
        [MethodImpl((MethodImplOptions)768)]
        [SuppressMessage("ReSharper", "JoinDeclarationAndInitializer")]
        [SuppressMessage("ReSharper", "CompareOfFloatsByEqualityOperator")]
        public unsafe NDArray ModDouble(in NDArray lhs, in NDArray rhs)
        {
            //lhs is NDArray of double
            switch (rhs.GetTypeCode)
            {
#if _REGEN1
                %op = "Mod"
                %op_bool = "*"
	            %foreach supported_dtypes, supported_dtypes_lowercase%
                case NPTypeCode.#1: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.#(op)(*((double*)lhs.Address), *((#2*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (#2*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
                        %foreach supported_dtypes,supported_dtypes_lowercase%
                        |#normalcast = ("("+str("#102")+")")
                        |#caster = ( "#102"=="bool" | ("Converts.To" + str("#101")) | ("double"=="bool"|("#2"=="bool"|("Converts.To" + str("#101"))|normalcast)| normalcast) )
	                    case NPTypeCode.#101: {
		                    var ret_address = (#102*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = #(caster)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = #(caster)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = #(caster)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = #(caster)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = #(caster)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = #(caster)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = #(caster)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = #(caster)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
                        %
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                %
                default:
		            throw new NotSupportedException();
#else

                case NPTypeCode.Boolean: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.Mod(*((double*)lhs.Address), *((bool*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (bool*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
	                    case NPTypeCode.Boolean: {
		                    var ret_address = (bool*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Byte: {
		                    var ret_address = (byte*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int32: {
		                    var ret_address = (int*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int64: {
		                    var ret_address = (long*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Single: {
		                    var ret_address = (float*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Double: {
		                    var ret_address = (double*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                case NPTypeCode.Byte: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.Mod(*((double*)lhs.Address), *((byte*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (byte*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
	                    case NPTypeCode.Boolean: {
		                    var ret_address = (bool*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Byte: {
		                    var ret_address = (byte*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int32: {
		                    var ret_address = (int*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int64: {
		                    var ret_address = (long*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Single: {
		                    var ret_address = (float*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Double: {
		                    var ret_address = (double*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                case NPTypeCode.Int32: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.Mod(*((double*)lhs.Address), *((int*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (int*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
	                    case NPTypeCode.Boolean: {
		                    var ret_address = (bool*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Byte: {
		                    var ret_address = (byte*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int32: {
		                    var ret_address = (int*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int64: {
		                    var ret_address = (long*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Single: {
		                    var ret_address = (float*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Double: {
		                    var ret_address = (double*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                case NPTypeCode.Int64: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.Mod(*((double*)lhs.Address), *((long*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (long*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
	                    case NPTypeCode.Boolean: {
		                    var ret_address = (bool*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Byte: {
		                    var ret_address = (byte*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int32: {
		                    var ret_address = (int*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int64: {
		                    var ret_address = (long*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Single: {
		                    var ret_address = (float*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Double: {
		                    var ret_address = (double*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                case NPTypeCode.Single: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.Mod(*((double*)lhs.Address), *((float*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (float*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
	                    case NPTypeCode.Boolean: {
		                    var ret_address = (bool*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Byte: {
		                    var ret_address = (byte*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int32: {
		                    var ret_address = (int*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int64: {
		                    var ret_address = (long*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Single: {
		                    var ret_address = (float*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Double: {
		                    var ret_address = (double*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                case NPTypeCode.Double: {
                    //if return type is scalar
                    var ret_type = np._FindCommonType(lhs, rhs);
                    if (lhs.Shape.IsScalar && rhs.Shape.IsScalar)
                        return NDArray.Scalar(Converts.ChangeType(Operator.Mod(*((double*)lhs.Address), *((double*)rhs.Address)), ret_type));
                    
                    (Shape leftshape, Shape rightshape) = DefaultEngine.Broadcast(lhs.Shape, rhs.Shape);
                    var lhs_address = (double*)lhs.Address;
                    var rhs_address = (double*)rhs.Address;
                    var retShape = leftshape.Clean();
                    var ret = new NDArray(ret_type, retShape, false);
                    var leftLinear = !leftshape.IsBroadcasted && !leftshape.IsSliced;
                    var rightLinear = !rightshape.IsBroadcasted && !rightshape.IsSliced;
                    switch (ret_type) {
	                    case NPTypeCode.Boolean: {
		                    var ret_address = (bool*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = Converts.ToBoolean(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = Converts.ToBoolean(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Byte: {
		                    var ret_address = (byte*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (byte)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (byte)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int32: {
		                    var ret_address = (int*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (int)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (int)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Int64: {
		                    var ret_address = (long*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (long)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (long)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Single: {
		                    var ret_address = (float*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (float)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (float)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    case NPTypeCode.Double: {
		                    var ret_address = (double*)ret.Address;
                            if (leftLinear && rightLinear) {
                                var len = ret.size;
                                Debug.Assert(leftshape.size == len && rightshape.size == len);
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    Parallel.For(0, len, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), (*(rhs_address + i)))));
                                }
                            } else if (leftLinear) { // && !rightLinear
                                if (rightshape.IsBroadcasted && rightshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var rval =  *rhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod((*(lhs_address + i)), rval)));
                                } else {
                                    int leftOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> rightOffset = rightshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset++)), (*(rhs_address + rightOffset(current)))));
                                    } while (incr.Next() != null);
                                }
                            } else if (rightLinear) { // !leftLinear && 
                                if (leftshape.IsBroadcasted && leftshape.BroadcastInfo.OriginalShape.IsScalar) {
                                    var lval =  *lhs_address;
                                    Parallel.For(0, ret.size, i => *(ret_address + i) = (double)(Operator.Mod(lval, (*(rhs_address + i)))));
                                } else {
                                    int rightOffset = 0;
                                    int retOffset = 0;
                                    var incr = new NDCoordinatesIncrementor(ref retShape);
                                    int[] current = incr.Index;
                                    Func<int[], int> leftOffset = leftshape.GetOffset;
                                    do {
                                        *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset++))));
                                    } while (incr.Next() != null);
                                }
                            } else {
                                int retOffset = 0;
                                var incr = new NDCoordinatesIncrementor(ref retShape);
                                int[] current = incr.Index;
                                Func<int[], int> rightOffset = rightshape.GetOffset;
                                Func<int[], int> leftOffset = leftshape.GetOffset;
                                do {
                                    *(ret_address + retOffset++) = (double)(Operator.Mod((*(lhs_address + leftOffset(current))), (*(rhs_address + rightOffset(current)))));
                                } while (incr.Next() != null);
                            }

                            return ret;
	                    }
	                    default:
		                    throw new NotSupportedException();
                    }
                }
                default:
		            throw new NotSupportedException();
#endif
            }
        }
    }
}
