# Generated benchmark outputs (recreated by run-benchmarks.ps1)
# README.md and benchmark-report.md are tracked (results documentation)
# JSON / CSV and the dashboard intermediate are ignored as machine-readable /
# regenerated outputs (the dashboard's tracked form is the DocFX page
# docs/website-src/docs/benchmarks-dashboard.md, seeded from this file)
/benchmark-report.json
/benchmark-report.csv
/benchmark-dashboard.md

# Benchmark run outputs
*.txt

# Python cache
__pycache__/
*.pyc


