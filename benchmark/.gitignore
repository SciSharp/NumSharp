# =============================================================================
# Benchmark Results (generated by run-benchmarks.ps1)
# =============================================================================

# Timestamped results directories (archived runs)
/results/

# Current results (copied from results/yyyyMMdd-HHmmss/ to benchmark/)
/benchmark-report.json
/benchmark-report.csv
/numpy-results.json
/numsharp-results.json

# README.md and benchmark-report.md are tracked (template/results documentation)

# =============================================================================
# Build & Runtime Artifacts
# =============================================================================

# Python cache
__pycache__/
*.pyc

# Log files (kept in results/ folders, not in root)
*.txt
